import React from "react";
import Card from "react-bootstrap/Card";

const StartContainer = () => {
  return (
    // <Card className="start">
    <Card.Body className="start">Start typing!</Card.Body>
    // </Card>
  );
};

export default StartContainer;
